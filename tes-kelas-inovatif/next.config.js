/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com", // Untuk Google avatar
      "i.imgur.com", // Untuk gambar dari Imgur
    ],
  },
  // Konfigurasi lainnya jika ada
};

module.exports = nextConfig;
